A loader creates nodes and edges from a source, and integrates them with the existing nodes and edges.


def loader(existing nodes and edges)
  read
  generate and integrate
  return updated nodes and edges
end
